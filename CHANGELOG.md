# Changelog

## 0.1.0 (2026-02-08)

Initial release.

- `PlaudClient` with recordings, transcriptions, speakers, and tags APIs
- Pydantic models for all API responses
- CLI tool (`plaud`) with full command set
- Token setup via CLI (`plaud auth setup`) and env var
